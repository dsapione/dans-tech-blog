<div class="container d-flex justify-content-center ">
	<div class="row row-cols-1 g-4 col-lg-8 mb-3">
		<div class="col">
			<div class="card">
				<h3 class="card-header">{{title}}</h3>
				<div class="card-body">
					<p>{{post_text}}</p>
					<p class="card-text">
							<small class="text-muted">Created by {{user.username}} on {{format_date created_at}}</small>
					</p>
				</div>
			</div>
		</div>	
	</div>
</div>		