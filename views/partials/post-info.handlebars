<a class="nav-link" href="/post/{{id}}">
	<div class="col">
		<div class="card">
			<h3 class="card-header">{{title}}</h3>
			<div class="card-body">
				<p class="card-text">
						<small class="text-muted">{{user.username}} on {{format_date created_at}}</small>
				</p>
			</div>
		</div>
	</div>		
</a>