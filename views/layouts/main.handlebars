<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="/stylesheets/style.css" />
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
	<title>Dan's Tech Blog</title>
</head>

<body>
	<div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
			<a href="/" class="h3 me-3 d-flex align-items-center nav-link text-dark">
				Dan's Tech Blog
			</a>
      <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
				{{#if loggedIn}}
        <li><a href="/dashboard" class="nav-link px-3 link-dark">Dashboard</a></li>
      </ul>
      <div class="col-md-3 text-end">
				<button type="button" id="logout" class="btn btn-primary me-2">Logout</button>
				{{else}}
        <a class="btn btn-outline-primary" href="/login" role="button">Login</a>
				{{/if}}
      </div>
    </header>
  </div>

	<main>
		{{{ body }}}
	</main>
 
  
  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>